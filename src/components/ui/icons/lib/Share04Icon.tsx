import { Icon, IconProps } from '@/components/ui/icons/Icon';
import { Share04Icon as Share04IconSvgr } from '../svg';

export const Share04Icon = (props: IconProps) => {
  return (
    <Icon {...props}>
      <Share04IconSvgr />
    </Icon>
  );
};
