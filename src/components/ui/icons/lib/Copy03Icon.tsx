import { Icon, IconProps } from '@/components/ui/icons/Icon';
import { Copy03Icon as Copy03IconSvgr } from '../svg';

export const Copy03Icon = (props: IconProps) => {
  return (
    <Icon {...props}>
      <Copy03IconSvgr />
    </Icon>
  );
};
