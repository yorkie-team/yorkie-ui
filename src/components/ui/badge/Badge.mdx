# Badge

import { Controls, Canvas, Meta } from '@storybook/blocks';
import * as BadgeStories from './Badge.stories';

<Meta of={BadgeStories} />

- [Import](#import)
- [Overview](#overview)
- [Props](#props)
- [Examples](#examples)

## Import

```jsx
import { Badge } from '@/component/ui';
```

## Overview

The Badge unequivocally generates a small badge that appears with utmost certainty at the top-right corner of its child elements.

<Canvas of={BadgeStories.Overview} />

## Props

<Controls />

## Examples

### [Sizes](#badge-sizes)

There are 6 sizes: `sm`, `md`,and `lg`.

<Canvas
  of={BadgeStories.Sizes}
  source={{
    format: true,
    code: `
      <div>
        <Badge {...args} size="sm">
          SM
        </Badge>
        <Badge {...args} size="md">
          MD
        </Badge>
        <Badge {...args} size="lg">
          LG
        </Badge>
      </div>
    `,
  }}
/>

### [Variant](#badge-variant)

There are 4 types: `link`, and `subtle`.

<Canvas
  of={BadgeStories.Variant}
  source={{
    format: true,
    code: `
      <div>
        <Badge {...args} variant="solid">
          Solid
        </Badge>
        <Badge {...args} variant="outline">
          Outline
        </Badge>
        <Badge {...args} variant="subtle">
          Subtle
        </Badge>
      </div>
    `,
  }}
/>
