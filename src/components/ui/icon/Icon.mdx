# Icon

import { Controls, Canvas, Meta } from '@storybook/blocks';
import * as IconStories from './Icon.stories';

<Meta of={IconStories} />

- [Import](#import)
- [Overview](#overview)
- [Props](#props)
- [Examples](#examples)

## Import

```jsx
import { Icon } from 'yorkie-ui';
```

## Overview

Menus empower users to take action with a single tap. Incorporate them into your design for a seamless and efficient user experience.

<Canvas
  of={IconStories.Overview}
  source={{
    format: true,
    code: `
      <Icon stroke="orange.default" icon={<IconCalendar />} />
    `,
  }}
/>

## Props

<Controls />

## Examples

### [Sizes](#icon-size)

There are seven sizes available for an Icon box: xs, sm, md, lg, xl, and 2xl. The default size is `md`.

<Canvas
  of={IconStories.Sizes}
  source={{
    format: true,
    code: `
      <Flex gap="6" align="center">
        <Icon {...args} size="xs" stroke="black.a6" icon={<IconCalendar />} />
        <Icon {...args} size="sm" stroke="black.a7" icon={<IconCalendar />} />
        <Icon {...args} size="md" stroke="black.a8" icon={<IconCalendar />} />
        <Icon {...args} size="lg" stroke="black.a9" icon={<IconCalendar />} />
        <Icon {...args} size="xl" stroke="black.a10" icon={<IconCalendar />} />
        <Icon {...args} size="2xl" stroke="black.a11" icon={<IconCalendar />} />
      </Flex>
    `,
  }}
/>
